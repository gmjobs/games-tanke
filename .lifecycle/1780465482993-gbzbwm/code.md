<!-- 代码骨架已写入工作区：坦克大战豪华版 -->

全部完成并验证通过：16 项单测全绿、单文件构建成功（48.8 KB）、评审修订的结算大字号已落实。

## 实现总结

基于已有的逻辑层（`src/logic.js`）与评审原型，我把原型补全为一个**完整、可直接运行**的工程，未留任何 TODO／占位。

### 创建 / 修改的文件

| 文件 | 职责 |
|------|------|
| **`index.html`**（新建） | 页面骨架 + 内联 CSS，完全沿用评审原型的 DOM 结构（主菜单/模式/设置/最高分/帮助/HUD/暂停/过关结算/结束结算/触屏/toast）。落实**评审修订**：游戏结束结算分数用独立 `.over-score` 类，相对原型 64px 放大到 **88px**（再放大 2 个字号）。通过两个 `<script>` 引入逻辑层与引擎层。 |
| **`src/game.js`**（新建，核心） | 引擎层：固定时间步长主循环（帧率无关 AC-3.4）、有限状态机、玩家四向移动/射击、敌人 AI（移动/转向/射击/趋向基地）、持续刷怪与同屏上限、子弹↔坦克/地形/基地/互相抵消碰撞、6 种道具（即时+限时计时与到期回滚提示）、计分/HUD、单关与通关/失败结算、暂停/失焦自动暂停、双人独立按键与分别计分、触屏控件、WebAudio 音效与 BGM、localStorage 持久化、全局异常友好提示。**全部规则从 `TankLogic` 取用**，不重复实现。 |
| **`src/logic.test.js`**（新建） | 逻辑层 Vitest 单测，16 用例覆盖网格常量、地形碰撞、AABB、坦克阻挡/防穿透、计分、关卡难度递增、地图生成确定性与安全区、敌人选型。 |
| **`package.json`**（新建） | 依赖清单 + 脚本：`dev`/`start`（本地服务器）、`build`（单文件交付）、`test`/`test:watch`。 |
| **`serve.js`**（新建） | 零依赖静态服务器，含防目录穿越，`npm run dev` → `localhost:5173`。 |
| **`build.js`**（新建） | 把 `index.html` + 两个 JS 内联合并为 `dist/index.html` 单文件交付包。 |
| **`README.md` / `.gitignore`**（新建） | 运行/测试/构建说明；忽略 `node_modules`、`dist`。 |
| `src/logic.js`（已存在，未改动） | 作为逻辑「单一事实来源」直接复用。 |

### 运行方式
- 直接双击 `index.html`（file:// 可玩）；或 `npm run dev`；或 `npm run build` 生成单文件。
- `npm install && npm test` 跑逻辑层单测。

与已评审的技术方案（分层解耦、数据驱动、固定步长、DOM+Canvas 混合、localStorage）和交互原型保持一致，未偏离已确认版本。
